---
'@crawfordyoung/ui': minor
---

`SidebarItem` gains an `asChild` prop (Radix Slot) — consumers can supply their own link element (e.g. `next/link`) for client-side navigation; item classes, `aria-current`, and internal icon/label markup merge into the child.
