---
'@crawfordyoung/ui': minor
---

Splash → app handoff: `startBrandHandoff` view-transition helper, `handoffName`/`exit` on `BrandSplash`, `handoffName` on `SidebarBrand`, tokenized view-transition CSS defaults. Browsers without the View Transitions API (and reduced-motion users) get the existing exit fade.
