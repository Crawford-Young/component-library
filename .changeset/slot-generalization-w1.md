---
'@crawfordyoung/ui': minor
---

Slot generalization W1 — extension convention + cheap evidenced slots: ChatPanel `header`/`actions`/`modal`/aside passthrough, ChatFab `icon`/`label`/`position`, EmptyState + HeroCard `renderCta`, SleepToggle `label`, Sidebar controlled `collapsed`/`onCollapsedChange`, Combobox `renderOption`, DatePicker/DateRangePicker `renderTrigger` + Calendar `classNames`/`components` pass-through, CountUp `format`, TopBar/PageHeader/SidebarBrand `title` widened to ReactNode. All additive.
